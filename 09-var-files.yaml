- name: variable from files
  hosts: local
  connection: local
  vars_files:
  - Area.yaml 
  tasks:
  - name: print Hello principal sir
    ansible.builtin.debug:
      msg: "Hello {{position}}, your start with carrer {{Experience}}, your from {{place}}"
